<?php

/**
 * addCommas
 *
 * @param int|string $numberWithoutCommas
 * @access public
 * @return string
 */
function addCommas($numberWithoutCommas) {

    // don't need an array for this, can use string position
    // first reverse the string so we are counting up
}
